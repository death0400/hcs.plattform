export module Enums {
    export enum UserStatus {
        Disabled = -1,
        None = 0,
        Active = 1
    }
    export enum PermissionStatus {
        Denied = -1,
        NotSet = 0,
        Granted = 1,
    }
}

