import { SyncWidthDirective } from './sync-width.directive';

describe('SyncWidthDirective', () => {
  it('should create an instance', () => {
    const directive = new SyncWidthDirective();
    expect(directive).toBeTruthy();
  });
});
