import { SyncCellWidthDirective } from './sync-cell-width.directive';

describe('SyncCellWidthDirective', () => {
  it('should create an instance', () => {
    const directive = new SyncCellWidthDirective();
    expect(directive).toBeTruthy();
  });
});
