import { PaddingLeftPipe } from './padding-left.pipe';

describe('PaddingLeftPipe', () => {
  it('create an instance', () => {
    const pipe = new PaddingLeftPipe();
    expect(pipe).toBeTruthy();
  });
});
